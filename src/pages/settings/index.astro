---
// The settings landing page previously showed a tile grid that duplicated
// the sidebar. It provided no information beyond what the sidebar already
// exposes, so visiting /settings now redirects to the first real page.
return Astro.redirect('/settings/general', 302);
---
